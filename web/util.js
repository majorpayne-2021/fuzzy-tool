// Shared utilities. Anything imported by multiple modules belongs here
// so the bundler doesn't get a top-level name collision.

export function escapeHtml(s) {
  return String(s)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}
